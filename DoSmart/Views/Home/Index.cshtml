@model List<DoSmart.Models.Activity>
@{
    ViewBag.Title = "Home Page";
}

<section class="content">    
    <div class="banner">

        <div class="banner-header"><h3>Proyectos</h3></div>

        <ul class="activities">
            <li>
                Primera Actividad
            </li>
            <li>
                Segunda Actividad
            </li>
        </ul>
    </div>
    <div class="details">
        <section class="subtasks">
        <h2>To do Activities</h2>
            <ul>
                @foreach (var activity in Model)
                    {
                    if (!activity.Done)
                    {
                        <li>
                            <h4>@activity.Title</h4>
                            <icon> </icon>
                            <button class="delete-button js-delete-activity" data-activity-id="@activity.Id">X</button>
                            @activity.Creator.Name           
                            <p>@activity.Content</p>
                            <div class="action-buttons">
                                <a href="@Url.Action("Edit", "Activities", new { id = activity.Id})">Edit</a>
                            </div>
                        </li>
                    }
                    }
            </ul>
        </section>
        <section class="subtasks">
            <h2>Done Activities</h2>
            <ul>
                @foreach (var activity in Model)
                {
                    if (activity.Done)
                    {
                        <li>
                            <h4>@activity.Title</h4>
                            @activity.Creator.Name
                            <p>@activity.Content</p>
                            <div class="action-buttons"><a href="#" class="js-delete-activity" data-activity-id="@activity.Id">Delete</a></div>
                        </li>
                    }
                }
            </ul>
        </section>
    </div>
</section>


@section scripts
{
    <script>
        $(document).ready(function () {
            $(".js-delete-activity").click(function (e) {
                var link = $(e.target);
                bootbox.dialog({
                    title: 'Confirm',
                    message: "Are you sure you want to delete this activity?",
                    buttons: {
                        cancel: {
                            label: "No",
                            className: 'btn-default',
                            callback: function () {
                                bootbox.hideAll();
                            }
                        },
                        ok: {
                            label: "Yes",
                            className: 'btn-danger',
                            callback: function () {
                                $.ajax({
                                    url: "/api/activities/" + link.attr("data-activity-id"),
                                    method: "DELETE"
                                })
                                    .done(function () {
                                        link.parents("li").fadeOut(function () {
                                            $(this).remove();
                                        });
                                    })
                                    .fail(function () {
                                        alert("Something failed!");
                                    });
                            }
                        }
                    }
                })
            })
        })
    </script>
}